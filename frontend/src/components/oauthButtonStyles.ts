/** Row wrapper aligned with GSI + Facebook login layout. */
export const oauthProviderRowClass =
  "flex min-h-[44px] w-full justify-center [&>div]:w-full";

/**
 * GSI `filled_black` + `large` (40px) + width 320 — shared by Google/Facebook custom chips.
 */
export const oauthButtonClass =
  "flex h-10 w-[320px] max-w-full shrink-0 items-center justify-center gap-3 rounded border border-solid border-[#8e918f] bg-[#131314] px-3 text-sm font-medium text-white transition-colors hover:bg-[#272727] disabled:pointer-events-none disabled:opacity-50";
